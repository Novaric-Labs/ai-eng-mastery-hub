// Copy to config.js and fill in. config.js is gitignored (it's not secret —
// the anon key is safe in the browser — but keep envs separate).
window.APP_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
  FUNCTIONS_BASE: "https://YOUR-PROJECT.functions.supabase.co",
};
